# Drive-SecureX
DriveSecureX (DSX) — Real-time ML-based intrusion detection system for vehicle CAN bus networks. Uses XGBoost (~99.8% accuracy, 93% cross-vehicle generalization) to detect DoS and Fuzzing attacks via ICSim, paired with a Flutter mobile app for live monitoring, instant alerts, and automated attack blocking.
